import 'package:flutter/material.dart';
class screenNoha extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
   return Scaffold(
     body: AppBar(title:Text("to do list"),),
   );
  }

}