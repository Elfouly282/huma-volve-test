import 'package:flutter/material.dart';

class TawasoulScreen extends StatelessWidget {
  const TawasoulScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('tawasoul Screen'),),
    );
  }
}