import 'package:flutter/material.dart';

class MarwaScreen extends StatelessWidget {
  const MarwaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Welcome marwa'),
      ),
    );
  }
}