
import 'package:flutter/material.dart';

class BasharShaqour extends StatelessWidget {
  const BasharShaqour({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: Text("hello"),
      ),
    );
  }
}
