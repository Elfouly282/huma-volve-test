import 'package:flutter/material.dart';

class EssamIbrahimScreene extends StatelessWidget {
  const EssamIbrahimScreene({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Essam Ibrahim Screene'),
      ),
      body: const Center(
        child: Text('Welcome to Essam Ibrahim Screene!'),
      ),
    );
  }
}