import 'package:flutter/material.dart';

class MariamScreen extends StatelessWidget {
  const MariamScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Mariam Screen'),
      ),
    );
  }
}